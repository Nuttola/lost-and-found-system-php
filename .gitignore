# .gitignore

# 1. ละเว้นไฟล์ที่ถูกอัปโหลด (Uploads)
# โฟลเดอร์ที่เก็บรูปภาพรายการที่ผู้ใช้แจ้งเข้ามา
uploads/

# 2. ละเว้นไฟล์การตั้งค่าฐานข้อมูล (Database Configuration)
# ไฟล์นี้มีรหัสผ่าน MySQL ซึ่งไม่ควรเผยแพร่ต่อสาธารณะ
config/db_connect.php
